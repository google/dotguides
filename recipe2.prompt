---
model: googleai/gemini-1.5-flash
input:
  schema:
    ingredients(array, list of ingredients):
      name: string, name of the ingredient
      quantity: string, quantity of the ingredient
output:
  schema:
    title: string, title of the recipe
    instructions(array, steps to make the recipe): string
    servings: number, how many people it serves
---

Generate a recipe that uses the following ingredients:
{{#each ingredients}}
- {{this.quantity}} of {{this.name}}
{{/each}}
